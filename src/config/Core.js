/**
 * This connects the most basic actions to classes.
 * Basically this enables the core functionality for a bare-naked editor, without any tools - anything else can be
 * configured more freely but this part just holds the bones of the core functionality via composition.
*/
class CoreConfig {

}